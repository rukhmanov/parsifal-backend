# Тестирование Yandex OAuth

## ✅ Настройка завершена

Учетные данные Yandex OAuth успешно настроены в файле `.env`:

```env
YANDEX_CLIENT_ID=043101fe4b31450cad5e3215c18512f2
YANDEX_CLIENT_SECRET=a255e79ba89c46b2a8a3cebe2c218496
YANDEX_CALLBACK_URL=http://localhost:8100/yandex-callback
```

## 🧪 Тестирование

### 1. Проверка эндпоинта обмена кода

Отправьте POST запрос на `/auth/yandex/token`:

```bash
curl -X POST http://localhost:3000/auth/yandex/token \
  -H "Content-Type: application/json" \
  -d '{
    "code": "test-code",
    "redirectUri": "http://localhost:8100/yandex-callback"
  }'
```

**Ожидаемый результат:** Сообщение об ошибке конфигурации больше не должно появляться. Вместо этого вы увидите ошибку от Yandex API о неверном коде (что нормально для тестового кода).

### 2. Проверка логов

В логах сервера теперь должно отображаться:
```
Получен запрос на обмен кода Yandex: { 
  code: 'test-code...', 
  redirectUri: 'http://localhost:8100/yandex-callback',
  clientId: 'установлен',
  clientSecret: 'установлен'
}
```

### 3. Полный тест аутентификации

1. Запустите фронтенд: `cd ../frontend && ionic serve`
2. Перейдите на страницу аутентификации Yandex
3. Выполните полный цикл аутентификации

## 🔧 Что было исправлено

1. **Добавлена проверка конфигурации** - теперь сервер проверяет наличие и корректность учетных данных
2. **Улучшено логирование** - подробные логи для диагностики проблем
3. **Обновлены учетные данные** - реальные Client ID и Client Secret из вашего приложения Yandex
4. **Исправлен Redirect URI** - соответствует настройкам в панели Yandex

## 📝 Следующие шаги

Теперь аутентификация через Yandex должна работать корректно. Если возникнут проблемы, проверьте логи сервера для получения подробной информации об ошибках.
